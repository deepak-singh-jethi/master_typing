# Phase 5 security and lint fix

- Updated `react-router-dom` from exact version `7.11.0` to exact version `7.18.2`.
- Removed the unnecessary escaped quote from the Phase 5 mobile-keyboard test regex.
- Kept the application on React Router declarative `HashRouter`; no route API migration was required.
