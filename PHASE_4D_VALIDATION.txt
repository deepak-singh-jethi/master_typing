# Phase 4D validation

## Automated regression suite

- 107 tests passed.
- 0 tests failed.
- Includes all typing-engine, adaptive-learning, curriculum, content, difficulty, account-isolation, cloud-sync, result-coaching, migration, storage, recovery, and rendering-window tests.
- Includes 10 dedicated Phase 4D tests.

## Phase 4D storage and recovery checks

- Detailed telemetry is removed from compact attempt summaries.
- Compact summaries merge correctly with IndexedDB records.
- Data version 8 migrates to version 9.
- Save operations write compact data to LocalStorage.
- IndexedDB save, read, prune, and clear transactions complete safely.
- Stale and incompatible active-session snapshots are rejected.
- Valid active sessions restore typed text, telemetry, elapsed time, and pace samples in a paused state.
- The offline outbox retains the latest 1,000 unique session IDs.

## Stress measurements

A simulated set of 1,000 detailed five-minute attempts measured approximately:

- detailed JSON: 11,511,522 bytes;
- compact LocalStorage JSON: 442,544 bytes;
- reduction: 96.16%;
- compact attempts retained: 1,000.

A 21,389-character target rendered a 1,809-character active window while keeping the active position inside the window.

## Static validation

- 79 JavaScript/JSX files parsed.
- 243 internal imports checked.
- 0 parse errors.
- 0 missing internal imports.
- 0 unused import/local diagnostics.
- 0 case-insensitive filename collisions.
- 14 route paths checked.
- 0 duplicate route paths.

## Package validation

- Full extracted package test suite passed.
- ZIP integrity passed.
- No `.env.local`, `node_modules`, `dist`, or `.git` content is included.
- No Supabase migration is required.

## Environment build note

The isolated package registry used in this environment returns 404 for `@fontsource-variable/geist@^5.3.0`. Therefore Oxlint and the Vite production build could not be installed or executed here.

Run the complete target-machine gate:

```bash
npm install
npm run check
npm audit
```

`npm run check` runs all Node tests, Oxlint, and the Vite production build.
